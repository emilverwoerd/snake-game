{
  "name": "neon-snake",
  "version": "1.0.0",
  "description": "Neon Snake — browser-based canvas snake game",
  "private": true,
  "scripts": {
    "serve": "npx serve . -l 3000 --no-clipboard",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "serve": "^14.2.0"
  }
}
